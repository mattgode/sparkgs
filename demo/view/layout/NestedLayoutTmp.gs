package view.layout

// Temp class until type literal coercion to structures works
class NestedLayoutTmp {
  function renderToString(body:String) : String {
    return NestedLayout.renderToString(body)
  }
}